#!/bin/bash
# Universal Cross-Platform Launcher for OrderProcessingSystem
# File: launch
# Works on: Windows (Git Bash/WSL), macOS, Linux

echo "🌍 OrderProcessingSystem - Universal Cross-Platform Launcher"

# Get the action from parameter or default to start
action="${1:-start}"

# Handle short flags and long parameters
case "$action" in
    "--s"|"--start") action="start" ;;
    "--x"|"--stop")  action="stop" ;;
    "--q"|"--qstart"|"--quick") action="qstart" ;;
    "--help"|"-h") 
        echo ""
        echo "📖 Usage:"
        echo "  ./launch [--s|--start|start]     Start with build validation"
        echo "  ./launch [--q|--qstart|qstart]   Quick start (no build validation)"
        echo "  ./launch [--x|--stop|stop]       Stop all services"
        echo ""
        echo "🔗 Short Keys:"
        echo "  --s   Start with build validation"
        echo "  --q   Quick start"
        echo "  --x   Stop all services"
        echo ""
        echo "🖥️ Platform Usage:"
        echo "  macOS/Linux: ./launch --s"
        echo "  Windows:     bash launch --s  (or just 'launch --s' in Git Bash)"
        exit 0
        ;;
esac

echo ""
echo "🎯 Action: $action"
echo ""

# Detect operating system
OS=$(uname -s)
case "$OS" in
    MINGW*|MSYS*|CYGWIN*)
        echo "🪟 Detected: Windows (Git Bash/MSYS)"
        echo "📂 Using Scripts-Windows folder for Windows system..."
        
        if [ ! -d "Scripts-Windows" ]; then
            echo "❌ Error: Scripts-Windows folder not found!"
            exit 1
        fi
        
        case "$action" in
            "start")
                echo "▶️  Executing: Scripts-Windows/start.ps1"
                powershell.exe -ExecutionPolicy Bypass -File "Scripts-Windows/start.ps1"
                ;;
            "stop")
                echo "▶️  Executing: Scripts-Windows/stop.ps1"
                powershell.exe -ExecutionPolicy Bypass -File "Scripts-Windows/stop.ps1"
                ;;
            "qstart")
                echo "⚡ Quick start mode"
                echo "▶️  Executing: Scripts-Windows/start.ps1"
                powershell.exe -ExecutionPolicy Bypass -File "Scripts-Windows/start.ps1"
                ;;
            *)
                echo "❌ Error: Unknown action '$action'"
                exit 1
                ;;
        esac
        ;;
    "Darwin"|"Linux"|*"BSD")
        echo "🍎 Detected: macOS/Unix-like system"
        echo "📂 Using Scripts-MacOS folder for Unix-like system..."
        
        if [ ! -d "Scripts-MacOS" ]; then
            echo "❌ Error: Scripts-MacOS folder not found!"
            exit 1
        fi
        
        chmod +x Scripts-MacOS/*.sh 2>/dev/null || true
        
        case "$action" in
            "start")
                echo "▶️  Executing: ./Scripts-MacOS/start.sh"
                ./Scripts-MacOS/start.sh
                ;;
            "stop")
                echo "▶️  Executing: ./Scripts-MacOS/stop.sh"
                ./Scripts-MacOS/stop.sh
                ;;
            "qstart")
                echo "⚡ Quick start mode (Unix-like only)"
                echo "▶️  Executing: ./Scripts-MacOS/qstart.sh"
                ./Scripts-MacOS/qstart.sh
                ;;
            *)
                echo "❌ Error: Unknown action '$action'"
                exit 1
                ;;
        esac
        ;;
    *)
        echo "❌ Error: Unsupported operating system: $OS"
        exit 1
        ;;
esac

echo ""
echo "✅ Command completed successfully!"
